[build]
  command = "pip install -r requirements.txt"
  functions = "netlify/functions"
  publish = "public"

[functions]
  directory = "netlify/functions"

[dev]
  command = "python3 -m uvicorn api.polish:app --reload --host 0.0.0.0 --port 8002"
  targetPort = 8002
